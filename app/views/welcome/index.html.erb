<div id="welcome">
    <% if !session['spotify_user'] %>
        <%= link_to "Sign in to Spotify", '/auth/spotify', class: "sign-in" %>
    <% else %>
    <% puts session[:spotify_user] %>
        
        <%= form_tag("/set_time", method:"post") %>
            <h2>Playlist Time</h2>
            <div id="time-wrap">
                <div id="maps-directions">
                    
                    <!-- GOOGLE MAPS DIRECTIONS LINK -->
                    <div id="maps-directions-link">
                        <%= link_to "Google Maps Directions", "#" %>
                    </div>
                    
                    <!-- GOOGLE MAPS DIRECTIONS INPUTS -->
                    <div id="maps-directions-input" class="hide">
                        <%= label_tag(:directions, "Starting point and destination.") %>
                        <%= text_field_tag(:start, :directions, placeholder: "Starting point, e.g. New York, New York") %>
                        <%= text_field_tag(:destination, :directions, placeholder: "Destination, e.g. Boston, Massachusetts") %>
                    </div>
                    
                </div>
                
                <div class="or">
                    <hr><h3>OR</h3><hr>
                </div>
                
                <div id="trip-time-input">
                    <%= label_tag(:time, "Trip time") %>
                    <%= number_field(:hours, :time, min: 0, max: 5, step: 1, placeholder: "Hours") %>
                    <%= number_field(:minutes, :time, min: 0, max: 55, step: 5, placeholder: "Minutes") %>
                    <%= submit_tag("Make playlist") %>
                </div>
            </div>
            
        <% #end %>
    <% end %>
</div>